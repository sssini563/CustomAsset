version: '3.3'

services:
  app:
    image: snipe/snipe-it:latest
    container_name: snipeit-app
    restart: unless-stopped
    ports:
      - "8008:80"
    env_file: .env.dc

    volumes:
      - ./app:/var/www/html/app
      - ./resources:/var/www/html/resources
      - ./routes:/var/www/html/routes
      - ./config:/var/www/html/config
      - ./database/migrations:/var/www/html/database/migrations
      - ./data/uploads:/var/www/html/public/uploads
      - ./data/storage:/var/www/html/storage
      - ./data/snipeit:/var/lib/snipeit
      - ./storage/framework/views:/var/www/html/storage/framework/views
      # ini penting! biar vendor bawaan image tidak ketimpa